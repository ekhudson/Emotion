using UnityEngine;
using System.Collections;

public class TileSprite : MonoBehaviour 
{

	// Use this for initialization
	void Start () 
	{
	
	}
	
//	public void OnCameraMoveEventHandler(object sender, CameraMoveEvent evt)
//	{
//		if (transform.position.x < TileManager.Instance.LeftBound ||
//			transform.position.x > TileManager.Instance.RightBound ||
//			transform.position.y < TileManager.Instance.UpperBound ||
//			transform.position.y > TileManager.Instance.LowerBound)
//		{
//			TileManager.Instance.RemoveTile(gameObject);
//		}
//	}
	
}
